[project]
name = "code-numerical"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "inspeqtor",
]

[tool.uv.sources]
inspeqtor = { git = "https://github.com/PorametPat/inspeqtor.git" }
